import type { Props } from "../components/ui/Cases.tsx";
import Component from "../components/ui/Cases.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
